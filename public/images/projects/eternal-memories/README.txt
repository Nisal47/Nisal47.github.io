Save the illustration you shared into this folder as:

  cover.jpg

Used as the project's cover image. It's a stylised illustration, not real
application footage, so the project page notes that alongside it (the
real demo is linked via video). Once the file is in this folder, the
site will pick it up automatically, no code changes needed.
